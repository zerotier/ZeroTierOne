[workspace]

members = [
  "zerotier-core-crypto",
  "zerotier-network-hypervisor",
  "zerotier-network-controller",
  "zerotier-system-service",
]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = 'abort'
